* Fixed past player names tooltip overflowing
* Improved syntax for calling scripter arguments to be more reliable
    * While the old tfivew.script(arg1,arg2) syntax still works, tfview.script[arg1|arg2] is now recommended